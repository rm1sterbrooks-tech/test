# 📝 Как создать файл .env для Telegram-бота

## Способ 1: Через PowerShell

Выполните в PowerShell:

```powershell
cd D:\test\telegram-bot
@"
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
PORT=8001
"@ | Out-File -FilePath .env -Encoding utf8
```

Затем откройте файл `.env` и замените значения на реальные.

## Способ 2: Вручную

1. Откройте папку `D:\test\telegram-bot`
2. Создайте новый файл с именем `.env` (без расширения)
3. Скопируйте туда:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
PORT=8001
```

4. Замените `your_bot_token_here` на токен от @BotFather
5. Замените `your_chat_id_here` на Chat ID от @userinfobot

---

## Где взять значения:

### TELEGRAM_BOT_TOKEN:
1. Откройте @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### TELEGRAM_CHAT_ID:
1. Откройте @userinfobot в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш `Id`

---

**После создания файла .env, сообщите мне, и мы перейдем к следующему шагу!**

