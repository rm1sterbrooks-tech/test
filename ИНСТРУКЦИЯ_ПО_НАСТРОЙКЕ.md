# 📋 Инструкция по настройке проекта

## Шаг 1: Настройка Telegram-бота

### 1.1 Создание бота

1. Откройте Telegram и найдите `@BotFather`
2. Отправьте команду `/newbot`
3. Укажите имя бота (например: "TicTacToe Game Bot")
4. Укажите username (должен заканчиваться на `bot`, например: `tictactoe_game_bot`)
5. Скопируйте токен (вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 1.2 Получение Chat ID

**Вариант 1: Через бота @userinfobot**
1. Найдите `@userinfobot` в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш `Id` (это и есть chat_id)

**Вариант 2: Через скрипт**
1. Добавьте токен в `telegram-bot/.env` как `TELEGRAM_BOT_TOKEN`
2. Отправьте вашему боту любое сообщение
3. Запустите скрипт:
   ```powershell
   cd D:\test
   py scripts\get_chat_id.py
   ```

### 1.3 Настройка .env файла

Создайте файл `telegram-bot/.env`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
PORT=8001
```

---

## Шаг 2: Установка зависимостей

### 2.1 Backend

```powershell
cd D:\test\backend
py -m pip install -r requirements.txt
```

### 2.2 Telegram-бот

```powershell
cd D:\test\telegram-bot
py -m pip install -r requirements.txt
```

### 2.3 Frontend

```powershell
cd D:\test\frontend
npm install
```

---

## Шаг 3: Настройка Backend

Создайте файл `backend/.env` (опционально, есть значения по умолчанию):
```env
TELEGRAM_BOT_URL=http://localhost:8001
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
```

---

## Шаг 4: Запуск проекта

### 4.1 Запуск Backend API

Откройте первый терминал:
```powershell
cd D:\test
.\backend\run.ps1
```

Или вручную:
```powershell
cd D:\test\backend
py -m uvicorn app.main:app --reload --port 8000
```

Backend будет доступен на: http://localhost:8000

### 4.2 Запуск Telegram-бота

Откройте второй терминал:
```powershell
cd D:\test
.\telegram-bot\run.ps1
```

Или вручную:
```powershell
cd D:\test\telegram-bot
py bot\main.py
```

Telegram-бот будет доступен на: http://localhost:8001

### 4.3 Запуск Frontend

Откройте третий терминал:
```powershell
cd D:\test
.\frontend\run.ps1
```

Или вручную:
```powershell
cd D:\test\frontend
npm run dev
```

Frontend будет доступен на: http://localhost:3000

---

## Шаг 5: Проверка работы

1. Откройте браузер: http://localhost:3000
2. Введите ваш Chat ID (если не введен)
3. Начните игру
4. При победе:
   - На экране появится промокод
   - В Telegram придет сообщение с промокодом
5. При проигрыше:
   - На экране появится предложение "Играть снова"
   - В Telegram придет сообщение "Проигрыш"

---

## Устранение проблем

### Backend не запускается
- Проверьте, что порт 8000 свободен
- Убедитесь, что установлены все зависимости

### Telegram-бот не отправляет сообщения
- Проверьте, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` правильно указаны в `.env`
- Убедитесь, что бот запущен на порту 8001
- Проверьте, что Backend может достучаться до Telegram-бота

### Frontend не подключается к Backend
- Убедитесь, что Backend запущен на порту 8000
- Проверьте CORS настройки в `backend/.env`

---

**Готово!** Проект должен работать. 🎉

