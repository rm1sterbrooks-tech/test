meta:
  environment: docker
  toolchain:
    name: docker-compose
    version: 2.22.0
build:
  dockerfile: Dockerfile
  context: .
run:
  persistence:
    - mountPoint: /app/data
      name: app_data
