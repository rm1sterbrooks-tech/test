# 🚀 Быстрый старт

## Шаг 1: Настройка Telegram-бота (5 минут)

1. Откройте Telegram → найдите `@BotFather`
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен
4. Найдите `@userinfobot` → отправьте сообщение → скопируйте ваш `Id`
5. Создайте файл `telegram-bot/.env`:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен
   TELEGRAM_CHAT_ID=ваш_chat_id
   PORT=8001
   ```

## Шаг 2: Установка зависимостей (5 минут)

Откройте 3 терминала и выполните:

**Терминал 1 - Backend:**
```powershell
cd D:\test\backend
py -m pip install -r requirements.txt
```

**Терминал 2 - Telegram-бот:**
```powershell
cd D:\test\telegram-bot
py -m pip install -r requirements.txt
```

**Терминал 3 - Frontend:**
```powershell
cd D:\test\frontend
npm install
```

## Шаг 3: Запуск (1 минута)

**Терминал 1 - Backend (порт 8000):**
```powershell
cd D:\test\backend
py -m uvicorn app.main:app --reload --port 8000
```

**Терминал 2 - Telegram-бот (порт 8001):**
```powershell
cd D:\test\telegram-bot
py bot\main.py
```

**Терминал 3 - Frontend (порт 3000):**
```powershell
cd D:\test\frontend
npm run dev
```

## Шаг 4: Играть! 🎮

1. Откройте браузер: http://localhost:3000
2. Введите ваш Chat ID (если не введен)
3. Начните игру!

---

## ✅ Проверка работы

- ✅ Backend доступен: http://localhost:8000
- ✅ Telegram-бот доступен: http://localhost:8001
- ✅ Frontend доступен: http://localhost:3000

---

## 🐛 Проблемы?

См. [`ИНСТРУКЦИЯ_ПО_НАСТРОЙКЕ.md`](ИНСТРУКЦИЯ_ПО_НАСТРОЙКЕ.md)

