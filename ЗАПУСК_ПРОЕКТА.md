# 🚀 Инструкция по запуску проекта

**Статус:** ✅ Все зависимости установлены!

---

## 📋 Шаг 1: Настройка Telegram-бота

**Если еще не настроен:**

1. Откройте Telegram → найдите `@BotFather`
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен (вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
4. Найдите `@userinfobot` → отправьте сообщение → скопируйте ваш `Id`
5. Создайте файл `telegram-bot/.env`:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен
   TELEGRAM_CHAT_ID=ваш_chat_id
   PORT=8001
   ```

**Или используйте скрипт для получения Chat ID:**
```powershell
cd D:\test
py scripts\get_chat_id.py
```

---

## 🚀 Шаг 2: Запуск всех компонентов

**Откройте 3 терминала PowerShell:**

### Терминал 1: Backend API (порт 8000)

```powershell
cd D:\test\backend
py -m uvicorn app.main:app --reload --port 8000
```

**Ожидаемый вывод:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### Терминал 2: Telegram-бот (порт 8001)

```powershell
cd D:\test\telegram-bot
py bot\main.py
```

**Ожидаемый вывод:**
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8001 (Press CTRL+C to quit)
```

### Терминал 3: Frontend (порт 3000)

```powershell
cd D:\test\frontend
cmd /c npm run dev
```

**Или используйте скрипт:**
```powershell
cd D:\test\frontend
.\run.ps1
```

**Ожидаемый вывод:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

---

## ✅ Шаг 3: Проверка работы

### 1. Проверьте Backend:
Откройте в браузере: http://localhost:8000

**Ожидаемый ответ:**
```json
{
  "message": "TicTacToe API",
  "version": "1.0.0"
}
```

### 2. Проверьте Telegram-бот:
Откройте в браузере: http://localhost:8001/health

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "bot_configured": true
}
```

### 3. Откройте игру:
Откройте в браузере: http://localhost:3000

**Должна открыться игра "Крестики-нолики"**

---

## 🎮 Шаг 4: Играть!

1. **Введите Chat ID** (если не введен) в поле на странице
2. **Начните игру** - кликайте по клеткам
3. **При победе:**
   - На экране появится промокод
   - В Telegram придет сообщение: "🎉 Победа! Промокод выдан: [код]"
4. **При проигрыше:**
   - На экране появится предложение "Играть снова"
   - В Telegram придет сообщение: "😔 Проигрыш"

---

## 🐛 Устранение проблем

### Backend не запускается
- Проверьте, что порт 8000 свободен
- Убедитесь, что зависимости установлены: `py -m pip install -r requirements.txt`

### Telegram-бот не отправляет сообщения
- Проверьте файл `telegram-bot/.env`
- Убедитесь, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` указаны правильно
- Проверьте, что бот запущен на порту 8001

### Frontend не запускается
- Используйте `cmd /c npm run dev` вместо `npm run dev`
- Проверьте, что Node.js установлен: `node --version`
- Убедитесь, что зависимости установлены: `cmd /c npm install`

### Frontend не подключается к Backend
- Убедитесь, что Backend запущен на порту 8000
- Проверьте консоль браузера (F12) на ошибки CORS
- Убедитесь, что в `backend/.env` указаны правильные `CORS_ORIGINS`

---

## 📝 Быстрые команды

### Запуск всех компонентов через скрипты:

**Backend:**
```powershell
cd D:\test\backend
.\run.ps1
```

**Telegram-бот:**
```powershell
cd D:\test\telegram-bot
.\run.ps1
```

**Frontend:**
```powershell
cd D:\test\frontend
.\run.ps1
```

---

**Готово! Проект должен работать!** 🎉

