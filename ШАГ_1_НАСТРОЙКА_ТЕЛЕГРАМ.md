# 📋 Шаг 1: Настройка Telegram-бота

## Что нужно сделать:

### 1. Создать бота через @BotFather

1. Откройте Telegram
2. Найдите `@BotFather`
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Укажите имя бота (например: "TicTacToe Game Bot")
   - Укажите username (должен заканчиваться на `bot`, например: `tictactoe_game_bot`)
5. **Скопируйте токен** (вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получить Chat ID

**Вариант 1: Через @userinfobot (проще)**
1. Найдите `@userinfobot` в Telegram
2. Отправьте любое сообщение
3. **Скопируйте ваш `Id`** (это и есть chat_id)

**Вариант 2: Через скрипт (после создания бота)**
1. Добавьте токен в `telegram-bot/.env`
2. Отправьте вашему боту любое сообщение
3. Запустите:
   ```powershell
   cd D:\test
   py scripts\get_chat_id.py
   ```

### 3. Заполнить файл .env

Откройте файл `telegram-bot/.env` и замените:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id_от_userinfobot
PORT=8001
```

---

## ✅ Проверка

После заполнения `.env` файла, можно проверить:

```powershell
cd D:\test\telegram-bot
py bot\main.py
```

Если всё правильно, вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8001
```

---

**Готовы продолжить?** После настройки Telegram-бота переходим к шагу 2 - запуску всех компонентов.

